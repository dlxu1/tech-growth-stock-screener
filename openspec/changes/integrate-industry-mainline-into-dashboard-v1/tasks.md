# 任务

## 1. 规格确认

- [x] Review 本 OpenSpec change，确认 v1 合并方向、默认行业选择和 dashboardv2 暂停策略。
- [x] 确认“行业的所有股票”优先指行业板块全成分股，而不是沪深 300 中的行业样本。
- [x] 确认首期行业切换采用服务端重算，而不是纯前端切换。

## 2. 行业主线能力复用

- [x] 将 `dashboardv2` 中行业主线对象构造逻辑抽到 dashboard 可复用模块。
- [x] 复用现有行业主线证据排序骨架，明确输入池切换后仍沿用同一套排序公式。
- [x] 为行业主线对象补充数据来源字段，区分行业全成分股、缓存样本、指数内样本。
- [x] 在行业主线对象中保留主线强度、涨幅、上涨家数占比、成交额、成长样本和回撤风险。
- [x] 增加缺失数据和降级提示，避免编造行业上涨原因。

## 3. v1 页面合并

- [x] 在 `dashboard` v1 数据健康模块下方插入行业主线证据板。
- [x] 将行业主线证据板放在高潜力与好时机矩阵上方。
- [x] 默认选中行业主线排名第一的行业。
- [x] 在行业证据板显示当前股票池来源、候选数量和降级状态。
- [x] 保留 v1 现有矩阵、详情、阶段表、回测和验证交互。

## 4. 行业股票池驱动下游阶段

- [x] 增加或明确 dashboard 参数，用于表达当前选中行业。
- [x] 默认未指定行业时，先计算行业主线榜并选中排名第一的行业。
- [x] 当行业全成分股可用时，用选中行业全成分股生成 `股票池`。
- [x] 当行业全成分股不可用时，降级为当前基础池内的行业样本，并在页面和 summary 中标注。
- [x] 确保 `宏观粗筛`、`技术分析`、`操作建议` 只消费选中行业股票池的下游结果。
- [x] 保持现有评分公式和阈值不变。

## 5. dashboardv2 暂停更新

- [x] 停止把新增交互需求放入独立 `dashboardv2` 页面。
- [x] 保留 `/dashboardv2` 或 CLI 兼容入口，避免历史链接突然失效。
- [x] 在文档或页面提示中标记 `dashboardv2` 已暂停更新，主入口回到 `dashboard`。
- [x] 调整测试重点，后续新增交互覆盖 v1。

## 6. 文档更新

- [x] 更新 `docs/data-rules.md`，记录行业股票池数据来源、降级规则和展示字段。
- [x] 更新 `docs/decisions.md`，记录行业主线合并进 v1、dashboardv2 暂停演进的决策。
- [x] 更新 `docs/handoff.md`，说明当前主看板入口和验证命令。
- [x] 必要时更新 `README.md` 的 dashboard 使用说明。

## 7. 验证

- [x] 增加 v1 HTML 位置测试：行业证据板在健康条之后、矩阵之前。
- [x] 增加默认行业选择测试：未指定行业时选中行业榜第一名。
- [x] 增加 pipeline 串行测试：下游股票均属于选中行业股票池。
- [x] 增加降级展示测试：行业全成分股不可用时标注缓存样本或指数内样本。
- [x] 运行 dashboard 相关单元测试。
- [x] 重新生成 `.cache/reports/dashboard_latest.html`。
- [x] 运行 `validate-dashboard --source cache` 检查数据健康与阶段串行关系。
